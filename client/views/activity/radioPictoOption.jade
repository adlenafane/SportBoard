template(name="radioPictoOption")
    label.small.radio-inline
        input.form-control(type="radio", name="#{group}", id="#{group}-#{label}")
        .picto(for="#{group}-#{label}", class="#{label}")