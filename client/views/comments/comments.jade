template(name="comments")
    .tile
        h2 Forum
        .comments-container
            each comments
                +commentItem

template(name="commentItem")
    .comment
        if gender
            img.female
        else
            img.male
        span
            strong #{author} :
            span #{text}
